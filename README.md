# nearby-locations-api

A small REST API that finds locations near a given point (each location has a service radius), plus detail lookup and dynamic upsert. Built with Fastify + TypeScript.

> Full documentation is added as the project is built — see `/docs` (Swagger UI) when the server is running.
